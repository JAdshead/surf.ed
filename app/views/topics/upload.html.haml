.devise-form
  %h2 Choose and Upload Video

  = form_tag @upload_info[:url], multipart: true do
    = hidden_field_tag :token, @upload_info[:token]
    .file-upload.btn
      .p Click to choose video
      = file_field_tag :file, {class: "uploadBtn upload", required: true}

    .form-button= submit_tag "Upload and submit"